import { SlicePairsPipe } from './slice-pairs.pipe';

describe('SlicePairsPipe', () => {
  it('create an instance', () => {
    const pipe = new SlicePairsPipe();
    expect(pipe).toBeTruthy();
  });
});
